# suit-help

Source-first extension components for shadcn-vue.

## Documentation

The documentation lives in `packages/docs` and is written in English.

```bash
vp run dev
vp run build
vp run ready
```

The source Registry lives in `packages/ui`. Its four current items are built
to `packages/docs/.docs/public/r` before the documentation starts or builds.
